{
  "name": "proto-typed",
  "displayName": "proto-typed",
  "description": "VSCode extension for the Proto-Typed.",
  "version": "0.0.1",
  "publisher": "proto-typed",
  "repository": {
    "type": "git",
    "url": "https://github.com/ricardoadorno/proto-typed"
  },
  "icon": "icons/icon-128.png",
  "engines": {
    "vscode": "^1.83.0"
  },
  "license": "Apache-2.0",
  "categories": [
    "Programming Languages",
    "Snippets"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onLanguage:proto-typed",
    "onCommand:proto-typed.showPreview"
  ],
  "contributes": {
    "languages": [
      {
        "id": "proto-typed",
        "aliases": [
          "Proto-Typed",
          "proto-typed"
        ],
        "extensions": [
          ".pty"
        ],
        "configuration": "./language-configuration.json",
        "icon": {
          "light": "./icons/icon-32.png",
          "dark": "./icons/icon-32.png"
        }
      }
    ],
    "grammars": [
      {
        "language": "proto-typed",
        "scopeName": "source.pty",
        "path": "./syntaxes/proto-typed.tmLanguage.json"
      }
    ],
    "snippets": [
      {
        "language": "proto-typed",
        "path": "./snippets/snippets.json"
      }
    ],
    "themes": [
      {
        "label": "Proto-Typed Dark Tokens",
        "uiTheme": "vs-dark",
        "path": "./themes/proto-typed-dark.json"
      }
    ],
    "commands": [
      {
        "command": "proto-typed.showPreview",
        "title": "Open Preview to the Side",
        "category": "Proto-Typed",
        "icon": {
          "light": "./icons/icon-24.png",
          "dark": "./icons/icon-24.png"
        }
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "proto-typed.showPreview",
          "when": "editorLangId == proto-typed",
          "group": "navigation"
        }
      ],
      "commandPalette": [
        {
          "command": "proto-typed.showPreview",
          "when": "editorLangId == proto-typed"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "pnpm run build:webview && pnpm --workspace-root run compile:core && pnpm --workspace-root exec tsc -p packages/extension/tsconfig.json",
    "build:webview": "cd webview && pnpm install && pnpm run build",
    "dev:webview": "cd webview && pnpm run dev",
    "watch": "tsc -watch -p ./",
    "watch:webview": "cd webview && pnpm run dev",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "pretest": "pnpm run compile",
    "test": "node ./dist/test/runTest.js",
    "test:update-snapshots": "cross-env UPDATE_SNAPSHOTS=1 pnpm test",
    "vsce:login": "npx @vscode/vsce login proto-typed",
    "vsce:package": "pnpm -w run compile && npx @vscode/vsce package",
    "vsce:publish": "pnpm -w run compile && npx @vscode/vsce publish",
    "vsce:publish:patch": "pnpm -w run compile && npx @vscode/vsce publish patch",
    "vsce:publish:minor": "pnpm -w run compile && npx @vscode/vsce publish minor",
    "vsce:publish:major": "pnpm -w run compile && npx @vscode/vsce publish major",
    "ovsx:publish": "pnpm -w run compile && npx ovsx publish --pat $OVSX_TOKEN"
  },
  "dependencies": {
    "@proto-typed/core": "workspace:*"
  },
  "devDependencies": {
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "@types/node": "^18.19.0",
    "@types/vscode": "^1.83.0",
    "@vscode/test-electron": "^2.3.8",
    "@vscode/vsce": "^3.6.2",
    "chai": "^4.3.10",
    "cross-env": "^7.0.3",
    "mocha": "^10.4.0",
    "pixelmatch": "^5.3.0",
    "playwright-core": "^1.56.1",
    "pngjs": "^7.0.0",
    "typescript": "^5.2.2"
  }
}
