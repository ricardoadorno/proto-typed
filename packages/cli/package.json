{
  "name": "@proto-typed/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "proto-typed": "dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts --format=esm --dts --clean",
    "dev": "tsx src/cli.ts",
    "lint": "eslint ."
  },
  "dependencies": {
    "@proto-typed/core": "workspace:*",
    "globby": "^14.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.6.0",
    "tsx": "^4.7.0",
    "eslint": "^9.0.0"
  }
}
