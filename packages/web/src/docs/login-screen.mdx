# Login Screen
## Pré-requisitos
- Conhecer `input` para campos de e-mail e senha.
- Definir rotas de suporte como recuperação de senha ou cadastro.
- Configurar ações de navegação (`@[Entrar]`, `@ghost[Esqueci]`) para conduzir o fluxo.

## Passo a passo
1. Estruture a tela com `container` e `card` para delimitar o formulário.
2. Adicione campos obrigatórios (`___email`, `___password`) com placeholders claros e validações.
3. Crie links auxiliares para recuperação e criação de conta.
4. Configure o botão principal para redirecionar ao dashboard ou próxima etapa segura.

<DocsCodePreview title="Tela de login completa">
```dsl
screen Login:
  container:
    # Acesse sua conta
    card:
      stack-loose:
        ___email: E-mail{usuario@empresa.com} | required
        ___password: Senha{Mínimo 8 caracteres} | required minlength="8"
        [X] Permanecer conectado

      row-end:
        @ghost[Esqueci a senha](Recover)
        @[Entrar](Dashboard)

screen Recover:
  container-narrow:
    # Recuperar senha
    ___email: E-mail cadastrado{usuario@empresa.com} | required
    row-end:
      @ghost[Cancelar](Login)
      @[Enviar instruções](sendReset)

screen Signup:
  container-narrow:
    # Criar conta
    stack-loose:
      ___: Nome completo{Seu nome}
      ___email: E-mail{email@empresa.com} | required
      ___password: Senha{Mínimo 8 caracteres} | required minlength="8"
    row-end:
      @ghost[Voltar](Login)
      @primary[Começar agora](createAccount)
```
</DocsCodePreview>
Por quê: organiza credenciais, links auxiliares e fluxos de suporte em uma única experiência.

## Depuração rápida
- Botão principal não navega → confirme o slug `Dashboard` no destino.
- Placeholder ausente → garanta `{placeholder}` em todos os inputs.
- Validação mínima ignorada → inclua `required` ou atributos como `minlength="8"`.
- Link de recuperação não retorna → ajuste `@ghost[Cancelar](Login)` para voltar à tela de login.
- Checkbox desalinhado → coloque `[X]` dentro do mesmo `stack` dos inputs.

## Próximos passos
- Revise [`Input`](./input) para cobrir campos adicionais (2FA, selects).
- Explore [`Modal Flow`](./modal-flow) se precisar autenticação em múltiplas etapas.
- Consulte [`Button`](./button) para definir variantes coerentes nas ações.
- Avalie [`Themes`](./themes) para aplicar branding à tela de login.
