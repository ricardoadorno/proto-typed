
# Começando com Proto-Typed DSL

import { DslExample } from "../../components/docs";

Proto-Typed é uma ferramenta de prototipagem rápida com uma DSL otimizada para LLMs. Edite à esquerda, visualize à direita.

---

## Conceitos Fundamentais

| Conceito      | Sintaxe                        | Uso                      |
|--------------|-------------------------------|--------------------------|
| **Screen**   | `screen Nome:`                | Telas/views da aplicação |
| **Modal**    | `modal Nome:`                 | Dialogs temporários      |
| **Drawer**   | `drawer Nome:`                | Menus laterais           |
| **Component**| `component Nome:`             | Blocos reutilizáveis     |
| **Container**| `container:`                  | Wrapper de conteúdo      |
| **Stack**    | `stack-tight:`                | Fluxo vertical           |
| **Row**      | `row-center:`                 | Fluxo horizontal         |
| **Grid**     | `grid-3:`                     | Grade de 3 colunas       |
| **Card**     | `card:`                       | Bloco destacado          |
| **Botão**    | `@[Texto](ação)`              | Ação clicável            |
| **Botão variante** | `@secondary-lg[Texto](ação)` | Variante + tamanho      |
| **Link**     | `#[Texto](destino)`           | Navegação inline         |
| **Input**    | `___email: Label{placeholder}`| Campo de formulário      |
| **Checkbox** | `[X] Label`                   | Checkbox marcado         |
| **Radio**    | `(X) Label`                   | Radio selecionado        |

---

## Primeira Tela

<DslExample title="Duas Telas" description="Navegação entre telas com botões">

```dsl
screen Home:
  container:
    # Bem-vindo ao Proto-Typed
    > Ferramenta de prototipagem rápida
    @[Ver Detalhes](Details)

screen Details:
  container:
    ## Detalhes
    > Conteúdo da segunda tela
    @[Voltar](Home)
```

</DslExample>

---

## Componente Reutilizável

<DslExample title="Componente" description="Declare uma vez, use em qualquer lugar">

```dsl
component Card:
  card:
    # Título do Card
    > Conteúdo reutilizável
    @[Ação](action)

screen Dashboard:
  container:
    $Card
    $Card
    $Card
```

</DslExample>

---

## Layout Canônico com Grid

<DslExample title="Grid de Cards" description="Organize elementos em grade">

```dsl
screen Dashboard:
  container:
    # Dashboard
    grid-3:
      card:
        ## Usuários
        # 1,234
      card:
        ## Vendas
        # R$ 45K
      card:
        ## Conversão
        # 23.5%
```

</DslExample>

---

## Formulário Simples

<DslExample title="Login" description="Inputs e botões">

```dsl
screen Login:
  container:
    card:
      # Login
      ___email: Email{seu@email.com}
      ___password: Senha{Digite sua senha}
      @[Entrar](login)
      @outline[Esqueci senha](recover)
```

</DslExample>

---

## Dicas Rápidas

- ✅ Use **2 espaços** para indentação
- ✅ Layouts canônicos: `container`, `stack-tight`, `row-center`, `grid-3`, `card`, `header`, `navigator`, `fab`
- ✅ Botões: `@[Texto](ação)` ou `@secondary-lg[Texto](ação)` (variante + tamanho)
- ✅ Inputs: `___email:`, `___password:`, `___textarea:` (tipos declarativos)
- ✅ Preview atualiza em tempo real!

---

## Próximos Passos

- 📖 Explore **Primitives** para elementos básicos
- 🎨 Veja **Layout System** para organizar conteúdo
- 🧩 Aprenda **Components & Props** para reutilização
- 🎯 Confira **Exemplos** para casos de uso reais
